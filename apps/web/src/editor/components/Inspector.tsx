"use client";

export function Inspector() {
  return (
    <div className="h-full w-full bg-muted/40 flex items-center justify-center">
      <span className="text-sm font-medium text-muted-foreground">PROPERTIES / Inspector</span>
    </div>
  );
}
