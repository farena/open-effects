# Next Steps

This document aims to collect and organize upcoming ideas and tasks for project development. Each proposed task should be discussed in detail with the user, considering opportunities to merge related items into a single actionable task to optimize planning and execution. Prioritization, consolidation, and task breakdown will be performed collaboratively before advancing with the work plan.

## Tasks

[] Audio track cut (with start and end time) and split into another track. Using same audio asset
[] Allow multiple audio track layers, all together grouped as AUDIO group (similar behavior than scene => layer)
[] Treat audio track same as layer, remove icon should be in the same spot, keyframes should be the same design, list of available props to be keyed + add keyframe button => list of keyframes added with options depending on type
[] Sidebar tabs vertical menu with icons + tooltip
[] Vertical adjustable border for top side in timeline, allow the user make the timeline bigger (max 45% viewport min 250px)
[] Topbar add button to go back to projects
[] API Documentation to allow agent to add animations
